const A = 1

enum Role {
    User
    Root
}

model User {
    Id: string
    Value: int64
}

service HttpUserService {
    GetById (userId: string) => (result: User)
    Upload (id: string, files: stream []byte)
}